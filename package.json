{
  "name": "muse_group",
  "homepage": "https://ZelArina.github.io/museGroup",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "predeploy": "npm run build", 
    "deploy": "gh-pages -d dist",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "start": "rsbuild dev"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.7.3",
    "@rsbuild/plugin-react": "^0.7.3",
    "gh-pages": "^6.1.1"
  }
}
